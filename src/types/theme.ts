export interface ThemeConfig {
  colors: {
    background: {
      primary: string;
      secondary: string;
      accent: string;
    };
    font: {
      primary: string;
      secondary: string;
      highlight: string;
    };
  };
}